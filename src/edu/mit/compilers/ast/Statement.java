package edu.mit.compilers.ast;

/** An Node of an AST that is a Decaf statement. */
public interface Statement extends Node {
	/*
	 * TODO(jasonpr): implement getChildBlocks() recursive depth of 1
	 * Blocks to return statements
	 * Statements to return blocks
	 */ 
}
